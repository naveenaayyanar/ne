"""
# Quick Build Summary

1. Create virtualenv, install deps from requirements.txt.
2. Run unit tests (not included in this skeleton).
3. Use PyInstaller for EXE packaging; sign builds for production.
4. Ensure dependencies (OpenCV, PIL, cryptography) are vendored and pinned.
"""
